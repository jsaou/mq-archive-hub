spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
  flyway:
    enabled: true
  jms:
    listener:
      auto-startup: false

logging:
  level:
    org.testcontainers: INFO
    com.github.dockerjava: WARN
